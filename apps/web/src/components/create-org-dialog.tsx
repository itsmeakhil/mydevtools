"use client"
import type { ReactNode } from "react"

export function CreateOrgDialog({
  open, onOpenChange,
}: { open: boolean; onOpenChange: (open: boolean) => void }): ReactNode {
  // ponytail: Task 14 ships the real form
  if (!open) return null
  return null
}
