# Backend workspace

This folder is reserved for the backend service.

Suggested next steps:

1. Initialize your preferred framework (e.g. Fastify, NestJS, Express).
2. Add runtime and lint/test scripts to `package.json`.
3. Wire shared packages under `packages/*` when needed.
